spring:
  cloud:
    nacos:
      config:
        group: DEFAULT_GROUP
        prefix: application
        server-addr: ${SPRING_CLOUD_NACOS_HOST:localhost}:8848
        file-extension: yml
      discovery:
        server-addr: ${SPRING_CLOUD_NACOS_HOST:localhost}:8848


